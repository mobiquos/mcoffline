<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block content_title %}Multicentro{% endblock %}</title>

    <link rel="icon" href="{{ asset('images/multicentro-favicon-32x32.png') }}" type="image/png">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            background-color: #f0f2f5;
            font-family: 'Segoe UI', sans-serif;
        }

        .tool-container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 0.5rem;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .tool-header {
            text-align: center;
            margin-bottom: 0.2rem;
        }

        .tool-header img {
            max-width: 200px;
            margin-bottom: 1rem;
        }

        .card-body {
            padding: 0.5rem;
        }
    </style>
</head>

<body>
    {% block container %}
    <div class="tool-container">
        <div class="tool-header">
            <div class="row">
                <div class="col col-5">
                    <img src="{{ asset('images/logo-text.png') }}" alt="Logo Multicentro">
                </div>
                <div class="col col-7 d-flex flex-column align-items-start justify-content-center">
                    <h3>{% block page_title %}{% endblock %}
                    </h3>
                </div>
            </div>
            <div class="row">
                <div class="col">
                    <p class="lead mb-0">Usuario: {{ app.user }}<span class="ms-3">Fecha: {{ "now"|date("d/m/Y")
                            }}</span><span class="ms-3">Fecha contigencia: {{ contingency.startedAt|date("d/m/Y")
                            }}</span></p>
                </div>
            </div>
        </div>

        <main>
            {% block form_content %}{% endblock %}
        </main>
    </div>
    {% endblock %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>

</html>
